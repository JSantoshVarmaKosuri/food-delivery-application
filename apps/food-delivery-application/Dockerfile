# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build food-delivery-application`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t food-delivery-application`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system food-delivery-application && \
          adduser --system -G food-delivery-application food-delivery-application

COPY dist/apps/food-delivery-application food-delivery-application
RUN chown -R food-delivery-application:food-delivery-application .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix food-delivery-application --omit=dev -f install

CMD [ "node", "food-delivery-application" ]
